# Copy the full output from kubeadm join command used in the control plane node.

# In worker nodes, paste the full kubeadm join command to join the cluster. Use sudo to run it as root.
# sudo kubeadm join...

# In the control plane node, view the cluster status
# kubectl get nodes
